{
  "name": "zen-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^1.2.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "rimraf": "^2.6.2"
  },
  "homepage": "http://dantejf88.github.io/appZen/",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "rimraf build/static/js/*.map && rimraf build/static/css/*.map && gh-pages -d build"
  }
}
